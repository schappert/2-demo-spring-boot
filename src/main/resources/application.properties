spring.application.name=books

# ==============================
# ? CONFIGURATION MYSQL
# ==============================

spring.datasource.url=jdbc:mysql://localhost:3306/shoppy?useSSL=false&serverTimezone=UTC
spring.datasource.username=tooba
spring.datasource.password=loulou
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==============================
# ?? CONFIGURATION JPA / HIBERNATE
# ==============================

# CrÃ©e ou met à jour automatiquement les tables
spring.jpa.hibernate.ddl-auto=update

# Affiche les requÃªtes SQL dans la console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ==============================
# ? PORT DU SERVEUR
# ==============================

server.port=8081

# ==============================
# ? ENCODAGE / LOCALISATION
# ==============================

spring.datasource.hikari.connection-init-sql=SET NAMES utf8mb4 COLLATE utf8mb4_unicode_ci
spring.output.ansi.enabled=ALWAYS

jwt.secret=MaSuperCleUltraSecretePourSignerLesTokens123456789
